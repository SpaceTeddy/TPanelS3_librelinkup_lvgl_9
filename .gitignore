.pio
.vscode
.dummy
managed_components
sdkconfig.defaults
sdkconfig.main

# Generated by pioarduino's IDF component manager, alongside the already
# ignored managed_components/ and .dummy/. Reappear on every pioarduino
# build, absent with the classic espressif32 platform.
CMakeLists.txt
dependencies.lock
