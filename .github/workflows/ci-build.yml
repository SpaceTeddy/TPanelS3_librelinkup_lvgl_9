name: PlatformIO Build

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "PlatformIO environment"
        required: true
        default: "main"
      run_tests:
        description: "Run PlatformIO unit tests (pio test)"
        required: true
        default: "false"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Cache PlatformIO
        uses: actions/cache@v5
        with:
          path: |
            ~/.platformio/.cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: pio-${{ runner.os }}-${{ hashFiles('platformio.ini') }}
          restore-keys: |
            pio-${{ runner.os }}-

      - name: Install PlatformIO Core
        # intelhex: tool-esptoolpy's bootloader-build step imports it but does
        # not install it itself - without this, "pio run" fails with
        # ModuleNotFoundError: No module named 'intelhex'.
        run: pip install --upgrade platformio intelhex

      - name: Show PlatformIO version
        run: pio --version

      - name: Build selected environment
        run: pio run -e "${{ github.event.inputs.environment }}"

      - name: Run unit tests (optional)
        if: ${{ github.event.inputs.run_tests == 'true' }}
        run: pio test -e "${{ github.event.inputs.environment }}"
